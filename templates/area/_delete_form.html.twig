<form method="post" action="{{ path('area_delete', {'id': area.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ area.id) }}">
    <button class="inline-flex items-center h-10 px-5 text-white transition-colors duration-150 bg-red-500 rounded-lg focus:shadow-outline hover:bg-red-600">Löschen</button>
</form>
