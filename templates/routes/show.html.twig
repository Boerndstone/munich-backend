{% extends 'base.html.twig' %}

{% block title %}Route{% endblock %}

{% block body %}
    <h1>Route</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ routes.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ routes.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('routes_index') }}">back to list</a>

    <a href="{{ path('routes_edit', {'id': routes.id}) }}">edit</a>

    {{ include('routes/_delete_form.html.twig') }}
{% endblock %}
