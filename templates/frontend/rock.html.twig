{% extends 'base.html.twig' %}

{% block title %}Munichclimbs | {% endblock %}

{% block body %}
    <div 
        class="container pb-2" 
        style="padding-top: 60px;"
    >
        <div id="main" class="mt-3">
            <div class="bg-white p-3 mb-4 flex items-center justify-between shadow">
                {# Eventuell gibt es Möglichkeit hier nur den Value für den Felsnamen abzurufen ohne ein Array durchzuschleifen!!! #}
                {% for headlineRock in rock %}
                    <h1 class="tracking-wide w-full">{{ headlineRock }}</h1>
                {% endfor %}      
            </div>
        </div>

    </div>
        
    {{ include('pageParts/footer.html.twig') }}
        
{% endblock %}